# pi-mcp 项目约定

## 本地运行日志

- 本地运行日志的查看者按拥有本机最高权限的运维者对待；不要增加逐次操作审批或日志查看权限分级。公网 MCP 的 OAuth 认证不属于这一显示策略，保持现有认证。
- 所有工具卡片的输入、输出和图片默认折叠，点击或 Enter 展开原始内容。不得按敏感文件名、密码、token、私钥等模式隐藏或脱敏正文。
- `write`、`edit`、`bash` 只用颜色和文字标签区分操作类别。
- 运行日志、工具输出和图片缓存仅存在有界内存中；不得自动写入日志文件、临时输出文件、TUI 调试文件或重启恢复文件。达到上限时淘汰最早内容，不溢写磁盘。
- 保留终端控制序列过滤与容量限制，不将控制字符当作指令执行；这不应改变普通可见文字。
- OAuth 密钥/客户端持久化与用户明确要求的文件写入属于应用状态和工具行为，不是运行日志，不要以“日志不落盘”为由删除或禁用。
- 不自动启用 shell 重定向、tee、tmux 录制、服务管理器日志持久化。不得承诺能够禁止外部软件录屏、收集 stdout 或操作系统交换内存。
